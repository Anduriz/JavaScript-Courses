var edad = 27;

// Existen tres formas de incrementar en 1 el valor de una variable
// edad = edad + 1;
// edad += 1;
// edad ++;

var peso = 75;

// peso = peso - 2;
peso -= 2;

var sandwitch = 1;

peso = peso + sandwitch;

var jugarAlFutbol = 3;

peso -= jugarAlFutbol;

var precioDeVino = 200.3;

var total = precioDeVino * 100 * 3 / 100;

 total = Math.round(precioDeVino * 100 * 3) / 100;

var  totalStr = total.toFixed(2);

var total2 = parseFloat(totalStr);

var pizza = 8;

var persona = 2;

var cantidadDePorcionesPorPersona = pizza / persona;